MCP vNext Rebuild

This branch introduces the MCP vNext validator and CI enforcement baseline.

- Validator: `tools/mcp-validator`
- CI workflow: `.github/workflows/mcp-validate.yml`
- Artifacts / example run: `mcp-run/mcp-output`

This PR documents and proposes the enforced MCP baseline for review.
