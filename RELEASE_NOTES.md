# Release Notes

- v1.0.1
  - Add Codex MCP clean scan flow documentation and GA overlay updates for BFF auth routes + admin db-ping policy.
- HUD will turn RED if the template performs a DB mutation without the mandatory audit wrapper/marker (Naked Mutation).
